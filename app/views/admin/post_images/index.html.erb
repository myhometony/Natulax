<%= render 'search_form', search:@search, url: admin_post_images_path %>

<% if @results.length !=0 %>
  <%= render 'index', post_images:@results %>
  <%= paginate @results %>
<% else %>
  該当する投稿が見つかりませんでした。
<% end %>
