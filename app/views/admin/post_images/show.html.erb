<div>
  <%= image_tag @post_image.get_image(300,300) %>
  <p>タイトル：<%= @post_image.title %></p>
  <p>カテゴリ：<%= @post_image.category.name %></p>
  <p>説明：<%= @post_image.caption %></p>
  <p>投稿ユーザー画像：<%= image_tag @post_image.end_user.get_profile_image(100,100),class:"rounded-circle border" %>
  <p>ユーザ名：<%= @post_image.end_user.name %></p>
  <p>投稿日：<%= @post_image.created_at.strftime('%Y/%m/%d') %></p>
    <%= link_to "削除", admin_post_image_path(@post_image), method: :delete,class:"btn btn-danger" %>
    <p>♥<%= @post_image.favorites.count %> いいね</p>
</div>

<div>
  <p>コメント件数：<%= @post_image.comments.count %></p>
  <% @post_image.comments.each do |comment| %>
    <p><%= image_tag comment.end_user.get_profile_image(100,100),class:"rounded-circle border" %></p>
    <%= comment.end_user.name %>
    <%= comment.created_at.strftime('%Y/%m/%d') %><%= comment.text %>
    <%= link_to "削除", admin_post_image_comment_path(comment.post_image, comment), method: :delete,class:"btn btn-danger" %>
  <% end %>
</div>

